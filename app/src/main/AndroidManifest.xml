<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="indah.com.lucious">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/kinstonaway4"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/kinstonaway4"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".splashScreen.SplashActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".list_view.ListActivity" />
        <activity android:name=".list_view.GGListActivity" />
        <activity android:name=".order_view.BTSActivity" />
        <activity android:name=".order_view.EXOActivity" />
        <activity android:name=".order_view.GOT7Activity" />
        <activity android:name=".order_view.RVActivity" />
        <activity android:name=".order_view.SNSDActivity" />
        <activity android:name=".order_view.GSDActivity" />
        <activity android:name=".About" />
    </application>

</manifest>